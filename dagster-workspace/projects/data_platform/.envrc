# Inherit workspace and root environment variables
source_up

dotenv_if_exists

watch_file pyproject.toml
watch_file uv.lock

# Override virtual environment with project-specific .venv
if [ -d ".venv" ]; then
  source .venv/bin/activate
fi
