# Inherit root environment variables (DAGSTER_HOME, DUCKDB_DATABASE, Nix flake, etc.)
source_up

watch_file deployments/local/pyproject.toml
watch_file deployments/local/uv.lock

if [ -d "deployments/local/.venv" ]; then
  source deployments/local/.venv/bin/activate
fi
